{
  "name": "myapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check",
    "format": "biome format --write",
    "test": "vitest",
    "test:run": "vitest run",
    "type": "tsc --noEmit"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
